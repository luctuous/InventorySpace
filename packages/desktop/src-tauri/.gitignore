target/

# Regenerated by `tauri build` from Cargo.toml and capabilities/. Three hundred
# kilobytes of machine-written JSON schema that would show up as noise in every
# diff, and that nobody reads.
gen/schemas/
